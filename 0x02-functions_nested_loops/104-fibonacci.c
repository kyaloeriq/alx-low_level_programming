#include <stdio.h>
/**
 * main - prints the first 98 Fibonacci numbers, starting with 1 and 2
 * Return: 0
 */
int main(void)
{
	int a, b, count, sum;

	sum = a + b;
	for (count > 0; count <= 98; count++)
	{
		printf("%d ,", sum);
		printf("\n");
	}
	return (0);
}


